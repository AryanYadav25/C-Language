#include <stdio.h>

int main() {
    int age = 10;
    if(age > 10){
        printf("You are eligible for the scheme");
    }
    if(age == 10){
        printf("You are eligible for the scheme");
    }
    else{
        printf("You are not eligible for scheme");
    }
    return 0;
}